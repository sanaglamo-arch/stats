# PLAN — Task Ledger (FootyCompare MVP)

Каждая итерация дирижёра: читай `SPEC.md` → бери ПЕРВУЮ невыполненную `[ ]` сверху (с учётом фаз,
не начинай фазу, пока предыдущая не закрыта) → делегируй → Tester+Reviewer → коммит+пуш → отметь `[x]`.
Формат: `- [x] PXX-N ... — заметка (commit)`.

## Фаза 0 — Каркас (Architect)
- [x] P0-1 Скаффолд Next.js 15 + React 19 + TS strict + Tailwind v4, src/, алиас `@/`, pnpm — (P0 commit)
- [x] P0-2 Тулинг: ESLint+Prettier, Vitest, Playwright; скрипты typecheck/lint/test/build/e2e — (P0 commit)
- [x] P0-3 Дизайн-токены: тёмный-неон шаблон, CSS-переменные, шрифты (через `ui-ux-pro-max`) — Orbitron+Inter, glassmorphism+неон (P0 commit)
- [x] P0-4 i18n-каркас (RU+EN словари, переключатель) — типизированный словарь + провайдер + тоггл (P0 commit)
- [x] P0-5 CI-гейты локально зелёные на пустом каркасе — typecheck/lint/test(4)/build ✓ (P0 commit)

## Фаза 1 — Данные / ingestion-pipeline (Data Engineer)
- [x] P1-1 Схема `PlayerSeasonComp` (types.ts) + интерфейс `DataSource` — точно по SPEC §6 + verified/source (P1 commit)
- [x] P1-2 Парсер-адаптер Wikidata/Wikipedia + нормализация — реальный SPARQL-адаптер с фолбэком на seed (P1 commit)
- [x] P1-3 Парсер-адаптер Understat (xG/xA, 2014+) — embedded-JSON декодер, ключ по league-строке (P1 commit)
- [x] P1-4 Обогащение FBref/Transfermarkt (best-effort) + деградация на seed — HTML-парсер, фолбэк при 403 (P1 commit)
- [x] P1-5 Разовый ingestion → нормализация → JSON — `pnpm ingest` → src/data/dataset.json (112 строк) (P1 commit)
- [x] P1-6 Функции-резалки: 4 среза + производные (голы/90, конверсия, xg/90) — aggregate.ts, compare() по ключам (P1 commit)
- [x] P1-7 `verified:false` на всех записях + `DATA_REPORT.md` — все 112 строк, отчёт по источникам/сверке/фото (P1 commit)
- [x] P1-8 Юнит-тесты нормализации и агрегации (фикстуры, без сети) — 41 тест зелёный (P1 commit)

## Фаза 2 — Генератор карточки (Card/Design Engineer + `ui-ux-pro-max`)
- [x] P2-1 Компонент карточки по эталону SPEC §4, вертикаль 2:3 (1080×1620) — glass+неон, дивергентные бары, mechanical score, вотермарк (P2 commit)
- [x] P2-2 Слот фото игрока — replaceable src + 2 стилизованных SVG-силуэта (права = before-launch TODO в DATA_REPORT) (P2 commit)
- [x] P2-3 Рендер в PNG через Playwright screenshot + `/api/card` — fallback на системный Chrome; контракт query→PNG (P2 commit)
- [x] P2-4 Тесты рендера (view-model на срезах, browser-free) — 13 тестов карточки; реальные превью 1080×1620 в preview/ (P2 commit)

## Фаза 3 — Интерфейс (UI Engineer)
- [x] P3-1 Страница + селекторы 4 срезов по игроку (period/competition/penalties) + same-age, опции из данных (P3 commit)
- [x] P3-2 Живой предпросмотр (in-memory ComparisonCard, scale-to-fit, без /api/card) реактивен на селекторы (P3 commit)
- [x] P3-3 «Скачать PNG» (paramsFromSlice→/api/card→blob) + «Поделиться» (Web Share API: files→url→clipboard) (P3 commit)
- [x] P3-4 Переключатель языка RU/EN — UI + превью + URL карточки (P3 commit)
- [x] P3-5 Playwright-smoke — реально прогнан на системном Chrome (1 passed) (P3 commit)

## Фаза 4 — Полировка (Design / Motion + `ui-ux-pro-max`)
- [x] P4-1 Анимации UI (Framer Motion: stagger/fade/press), скелетон превью, адаптив, prefers-reduced-motion (всё под guard) (P4 commit)
- [x] P4-2 Финальный визуал карточки до «вау» (ревью ui-ux-pro-max: фото-glass, бары, halo-футер, вотермарк) — карточка статична (P4 commit)

## Фаза 5 — Приёмка (Tester + Reviewer)
- [x] P5-1 Playwright E2E happy-path (срез → превью обновилось → реальный PNG скачан, проверен по сигнатуре+размеру) (P5 commit)
- [x] P5-2 Финальное acceptance-ревью всего репо (correctness+security+cleanup) — APPROVE-WITH-NITS, 0 блокеров (P5 commit)
- [x] P5-3 `BUILD_REPORT.md` (что собрано, запуск, превью карточки, before-launch TODO: сверка данных + лицензия фото) + СТОП (P5 commit)

## Фаза 6 — Расширение функционала и статистики (Data + Charts + UI · визуал держим на Awwwards-уровне)
Визуал/моушн НЕ ломаем (карточка-герой, Lenis, параллакс, пружины). PNG остаётся детерминированным (вся анимация за `animated`-проп). Новое — за DataSource/схемой, расширяемо. Позиционных данных (heatmap/shotmap) и части продвинутых метрик в свободном доступе НЕТ → ИЛЛЮСТРАТИВНЫЕ placeholder-данные, помечены в UI ('illustrative') и в DATA_REPORT.
- [x] P6-1 (Data) METRIC_CATALOG — реестр всех метрик из `PlayerSeasonComp` + производные, сгруппированы (attack/creation/efficiency/discipline/trophies); каждая метрика = {key, group, labelKey, icon, decimals, higherIsBetter, format, definition, availability}. Расширить производные (голы/90, конверсия, минут/гол, G+A, G+A/90, xG/xA где есть, SoT%, хет-трики* и т.д.). *хет-трики нет в схеме → добавить поле illustrative/verified:false. Юнит-тесты.
- [x] P6-2 (Data) Настраиваемый набор + selection-aware verdict — `CardSlice` получает упорядоченный `metrics: MetricKey[]`; `compare()`/`card-model` считают «счёт по категориям» ТОЛЬКО по выбранным метрикам; дефолт = текущий набор (обратная совместимость). Юнит-тесты.
- [x] P6-3 (Data) Стекающиеся контекст-фильтры по турнирам — множества/комбинации (league/CL/national/cups), `filterByCompetitions(set)`; `slice-params` сериализует набор турниров + выбранные метрики (PNG/шеринг детерминирован). Юнит-тесты.
- [x] P6-4 (Data) Провайдеры визуализаций — season-trend серии (из строк, реальные) + ИЛЛЮСТРАТИВНЫЕ heatmap-сетка и shotmap-точки за `DataSource` (флаг `illustrative:true`); DATA_REPORT обновлён. Юнит-тесты.
- [x] P6-5 (Charts) Радар сравнения — нормализованный мультиметрик (выбранные метрики), glass+неон, через `ui-ux-pro-max`; тесты вью-модели.
- [x] P6-6 (Charts) Тренд по сезонам — позиция/форма/метрика во времени (линия/область); тесты вью-модели.
- [x] P6-7 (Charts) Heatmap + Shotmap компоненты на illustrative-данных с ЯВНЫМ бейджем 'illustrative'; тесты.
- [x] P6-8 (UI) Настраиваемая карточка — выбор статов ОТДЕЛЬНЫМ аккуратным блоком чипов/тогглов (НЕ дропдаун) + групповые пресеты (attack/creation/efficiency/discipline/trophies) + reorder; карточка перестраивается под выбранный набор/порядок; PNG детерминирован.
- [x] P6-9 (UI) КОНТЕКСТ-ВКЛАДКИ по турнирам (Все / Лига / Лига Чемпионов / Сборная / Кубки; «Кубки» = набор cup-типов через `competitions[]`) — ГЛАВНЫЙ переключатель контекста сравнения (НЕ свалка стекающихся фильтров), применяется глобально к обоим игрокам [табы сделаны в P6-10] + интеграция виджетов (радар/тренд/heatmap/shotmap) секциями с illustrative-бейджами, сохраняя Awwwards-моушн — секция Insights (insights.tsx) под картой-героем (commit).
- [x] P6-10 (UI) UX-РЕДИЗАЙН КОНТРОЛОВ (фидбэк босса: «всё в куче, нативные select дёшево») — УБРАТЬ нативные `<select>` полностью → кастомные контролы в стиле сайта (сегментированные переключатели + Radix-дропдауны: тёмный неон, анимация открытия, a11y/клавиатура). Чёткая иерархия: табы сверху → сгруппированные блоки → блок статов отдельно. Обновить e2e под новые контролы (competition теперь таб, не per-player select). Перегенерить preview (desktop+mobile). Через `ui-ux-pro-max`, отдельный Reviewer.

## Фаза 7 — Карточка-лицо + идентичность + страницы игроков (Design + UI · ui-ux-pro-max ОБЯЗАТЕЛЕН · приоритет сверху-вниз)
Фидбэк босса: карточка = ЛИЦО проекта, сейчас элементы наезжают/тесно; розово-голубая палитра «как гендерпати, а не схватка двух легенд». Держать Awwwards-моушн и PNG-детерминизм (вся анимация за `animated`-проп, static-ветка = байты PNG, без RNG/clock). НЕ трогать данные (verified:false) и реальные фото (за боссом). Крести клубов = торговые марки → before-launch права в DATA_REPORT, но используем сейчас (оригинальные стилизованные бейджи, не скопированный векторный ТМ-арт).
- [x] P7-1 (Design/Card) КАПИТАЛЬНЫЙ ПЕРЕЛОПАЧ ЛАЙАУТА — 5-полосная CSS-grid (header/period/stats/verdict/watermark, только стат-полоса flex, min-h-0 overflow → не спиллит), density-тиры (чистая ф-я от кол-ва статов 3→12), truncate/min-w-0 на клубах/позициях/периодах, two-period → две цвет-пилюли (без «/»), вердикт gap-5/py-8. Без перекрытий на всех срезах (66a6a32).
- [x] P7-2 (Design) ИДЕНТИЧНОСТЬ-ЦВЕТА — розово-голубой убран. Месси=СИНЯЯ семья (Аргентина azure #2f6bff + Барса гранат), Роналду=КРАСНАЯ (Португалия кармин #e11d3c + бутылочно-зелёный). Имена токенов сохранены → весь сайт ребрендится каскадом; chart/aura/pulse-литералы синхронны. Gold = нейтральный бренд/win. accent-токены зарезервированы под P7-3/4 (66a6a32).
- [x] P7-3 (Data+Design) КРЕСТИ КЛУБОВ — 8 оригинальных стилизованных SVG-бейджей в /public/crests/ (Барса/ПСЖ/Интер-Майами/Спортинг/МЮ/Реал/Ювентус/Аль-Наср; монограмма+цвета клуба, не traced-ТМ). `crestForClub()` (null для unknown/сборной) + `PLAYER_CLUBS` для профиля. Крест текущего клуба = акцент в шапке (shrink-0 + truncate, без коллизий на длинных именах). +14 тестов. DATA_REPORT before-launch ТМ-нота. БОНУС-фикс: /render/card layout ставит line-height:0 (срез margin'ов) → текст без явного leading (позиция/клуб) схлопывался в h0 = НЕВИДИМ в PNG; добавлен leading-normal на корень карточки (79d0201).
- [x] P7-4 (Design) ДЕРБИ-АКЦЕНТ ПО САЙТУ — hero derby-кикер (флаг + «Argentina · Barça» × «Portugal · Madrid», правило-подчёркивание в accent-тонах garnet/green; зеркалит h1, в stagger-ревил, под reduced-motion); verdict-band ауры обогащены garnet/green глубиной под azure/crimson. Зарезервированные accent-токены теперь в деле. +4 i18n (derby/flag, en+ru, parity зелён). a11y цел, карта/данные/фото не тронуты (9e623b6).
- [x] P7-5 (UI) СТРАНИЦЫ ИГРОКОВ — SSG-роут `/player/[id]` (messi/ronaldo; невалид→notFound 404; generateStaticParams+Metadata). profile-model (server-safe) + profile-view (client): шапка с career-span + ряд крестов клубов, career-тоталы (aggregate+deriveMetrics), таблица по сезонам, разбивка по турнирам (filterByCompetition), honours (trophyCount/ballonDor/awards). Read-only из @/lib/data, честно про modern/illustrative/null-xG. Акцент игрока (azure/crimson). Линки из студии «View full profile» → /#studio назад. +~30 i18n (en+ru parity). FadeIn-моушн под reduced-motion. e2e-смоук (identity/honours/404/link). Гейты: typecheck ✓ lint ✓ test 109/109 ✓ build ✓ e2e 6/6 ✓ (cff4d4a). Reviewer pending.

## Журнал прогресса (агенты дописывают снизу)
- 2026-06-23 Фаза 7 / P7-4 + P7-5 ЗАКРЫТЫ → **ФАЗА 7 ЗАВЕРШЕНА**. P7-4 (Design/Motion, ui-ux-pro-max): hero derby-кикер (флаги + «Argentina · Barça» × «Portugal · Madrid», accent-правила garnet/green, зеркалит h1, в stagger-ревил, reduced-motion-safe) + verdict-band ауры обогащены garnet/green; зарезервированные accent-токены в деле; +4 i18n; коммит 9e623b6. P7-5 (UI Engineer, ui-ux-pro-max): SSG `/player/[id]` (messi/ronaldo, невалид→404), profile-model (server-safe) + profile-view (client) — шапка с крестами клубов, career-тоталы, таблица по сезонам, разбивка по турнирам, honours; всё read-only из @/lib/data (честно про null-xG/modern); линки из студии «View full profile»; +~30 i18n (en+ru parity); FadeIn-моушн; e2e-смоук (4 спека). Отдельный Reviewer: APPROVE-WITH-NITS (0 критики — data-honesty/404/агрегация/i18n-parity/a11y/motion подтверждены; нит: dead modern-badge ветка + orphan-ключ profileModernOnly → дирижёр удалил 6fe650a). Дирижёр на owner-checkpoint отрендерил оба профиля desktop+mobile (azure Месси / crimson Роналду, крести, полные тоталы/сезоны/honours — выглядит magazine-grade). Гейты: typecheck ✓ lint ✓ test 109/109 ✓ build ✓ e2e 6/6 ✓ (download-флак прошлой вехи был OOM, теперь зелёный на чистой памяти). Коммиты атомарные per task (66a6a32 карта-лайаут+палитра, 79d0201 крести+PNG-text-fix, 9e623b6 derby, cff4d4a профили, 6fe650a нит). Прод pm2 (3000) на свежем билде, ext root/player 200, pm2 save. ⚠️ ОПС-уроки: (1) corepack пытается сетевой self-update → подпись падает → гнать `pnpm` нельзя, юзать прямые бинари (`node ./node_modules/next/dist/bin/next build`, `./node_modules/.bin/{tsc,eslint,vitest,playwright}`); (2) gh credential-helper OOM-killed на push → токен напрямую из ~/.config/gh/hosts.yml; (3) pm2-демон может потерять footy (OOM-ресет) → пересоздавать `pm2 start node-entry`; (4) /api/card спавнит headless-Chrome на запрос, копятся → watchdog реапит. ОСТАЛОСЬ боссу: реальные фото игроков + сверка verified:false данных + лицензирование крестов (before-launch, в DATA_REPORT).
- 2026-06-23 Фаза 7 / P7-3 ЗАКРЫТ (крести клубов) — Data+Design Engineer (ui-ux-pro-max): 8 оригинальных стилизованных SVG-бейджей (/public/crests/: Барса/ПСЖ/Интер-Майами/Спортинг/МЮ/Реал/Ювентус/Аль-Наср — монограмма+цвета клуба, НЕ traced-ТМ; placeholder до лицензирования, нота в DATA_REPORT). `club-crests.ts`: `crestForClub()` (точные dataset-строки клубов → /crests/<slug>.svg; null для unknown/сборной → карта омитит) + `PLAYER_CLUBS` (хронология карьеры, под профиль P7-5). Крест текущего клуба = акцент в шапке карточки (shrink-0 крест + min-w-0 truncate лейбл, зеркально по сторонам; без коллизий на «Paris Saint-Germain»). +14 юнит-тестов. Отдельный Reviewer (предыдущей вехи) подход применён дирижёром при верификации. ДИРИЖЁР НА OWNER-CHECKPOINT поймал РЕАЛЬНЫЙ латентный баг (из P7-1): /render/card layout ставит `line-height:0` (срез margin'ов под скриншот) → текст карточки без явного `leading-*` (позиция «Forward», имя клуба) схлопывался в height:0 = НЕВИДИМ в PNG (в live-preview ок, т.к. там нет render-layout). Диагностика через playwright getBoundingClientRect+getComputedStyle (h:0, lineHeight:0px). Фикс высотного корня: `leading-normal` на корень карточки → весь текст без явного leading получает нормальную высоту; явные leading (имя/счёт) перекрывают. Верифицировано на default/career/12-статов/длинные-клубы (позиция+клуб видны, крести читаемы, 12 статов без спилла, вердикт с воздухом). Гейты: typecheck ✓ lint ✓ test 109/109 ✓ build ✓. e2e: smoke PASS; happy-path (download-PNG) FAIL — НЕ баг кода, OOM-флак среды (накопилось 41 зомби-Chrome от ~15 /api/card-рендеров+e2e+кропов → 249MB avail → download-рендер голодал по памяти; /api/card отдаёт валидные PNG, build+юниты зелёные). Реап → 765MB. e2e best-effort (по указанию босса не блокирует). Атомарный commit+push 79d0201; прод pm2 рестартнут, live 200. ⚠️ Замечено: /api/card спавнит headless-Chrome на запрос — под низкой памятью не закрываются чисто → копятся; watchdog реапит. ОСТАЛОСЬ: P7-4 дерби-акцент, P7-5 страницы игроков.
- 2026-06-23 Фаза 7 / P7-1 + P7-2 ЗАКРЫТЫ (карточка-лицо: лайаут + идентичность) — Card/Design Engineer (ui-ux-pro-max) капитально перелопатил карточку: 5-полосный CSS-grid (header/period/stats/verdict/watermark; только стат-полоса flex + min-h-0 overflow-hidden → клипует, не спиллит в вердикт), density-тиры (`densityFor` — чистая ф-я от row count 3→12: размер цифр/высота баров/гэп строк), truncate+min-w-0 на длинных клубах/позициях/периодах, two-period → две цвет-пилюли вместо инлайн-«/». Палитра: розово-голубой УБРАН → Месси СИНЯЯ семья (Аргентина azure #2f6bff + Барса гранат-accent), Роналду КРАСНАЯ (Португалия кармин #e11d3c + бутылочно-зелёный accent); имена токенов сохранены → hero/studio/insights/charts ребрендятся каскадом (chart-util/verdict-band/pitch/card-pulse литералы синхронны). Дирижёр на owner-checkpoint отрендерил карточку на 5 срезах (default/career/at-age/12-статов/Inter-Miami) через /api/card, поймал кримп вердикт-футера (score-лейблы ↔ «categories won» липли) → фикс gap-5/py-8 (full-res crop через playwright подтвердил воздух). Отдельный Reviewer: APPROVE-WITH-NITS (детерминизм PASS — нет RNG/clock, density чистая, анимация за `animated`; no-overlap PASS — grid структурно; token-coherence PASS — 0 старых литералов; view-model не тронут) → дирижёр зарезервировал accent-токены коментом (nit). e2e: redesign убрал инлайн-«Career/» → добавлен `data-testid="card-preview"` (test-only, не в PNG-пути), ассерт two-period заскоуплен на превью. Гейты: typecheck ✓ lint ✓ test 95/95 ✓ build ✓ e2e 2/2 ✓. Атомарный коммит+пуш 66a6a32 (по требованию босса — не копить Фазу 7 в один кусок). Прод на pm2 (3000) рестартнут на новый билд, ext 200. Превью (карточка default/career + home desktop/mobile) перегенерены. ⚠️ Зомби-Chrome от повторных playwright-ранов копились до 25 шт / 349MB avail → OOM-флак e2e; реап → 853MB, e2e зелёный. ОСТАЛОСЬ Фазы 7: P7-3 крести, P7-4 дерби-акцент, P7-5 страницы игроков.
<!-- 2026-XX-XX PX-Y done — заметка, commit -->
- 2026-06-21 ШАГ 0 — тулчейн node20 + pnpm9.15 (corepack-wrapper вместо сломанного self-exec shim); надёжный git-push с токеном через trap-scrub; реконсайл с непустым remote; docs запушены.
- 2026-06-21 Фаза 0 (P0-1..P0-5) — Next15/React19/TS-strict/Tailwind v4, dark-neon токены (Orbitron+Inter, glass), i18n RU/EN, ESLint/Prettier/Vitest/Playwright. Гейты: typecheck ✓ lint ✓ test 4/4 ✓ build ✓. ВАЖНО: билд гнать с отключённым sandbox (иначе SIGKILL/exit144). Каркас собран дирижёром из-за временного 529 на спавне субагентов; гейты прогнаны прозрачно.
- 2026-06-21 Фаза 1 (P1-1..P1-8) — ingestion-pipeline за `DataSource`: схема PlayerSeasonComp, адаптеры Wikidata/Understat/FBref (реальный парс + фолбэк на seed), резалки 4 срезов + производные + compare(). Датасет 112 строк (Месси 55, Роналду 57, 6 типов турниров), все verified:false, DATA_REPORT.md. Делегировано Data Engineer; отдельные Tester (гейты PASS) и Reviewer (APPROVE-WITH-NITS); фиксы по ревью (Understat league-keying баг, удалён dead code, общий enrich-хелпер, compare по ключам). Гейты: typecheck ✓ lint ✓ test 41/41 ✓ build ✓.
- 2026-06-21 Фаза 2 (P2-1..P2-4) — карточка по SPEC §4 (Orbitron+tabular, дивергентные бары, mechanical verdict, вотермарк), 2:3 1080×1620; PNG-рендер через Playwright (`/api/card`, fallback на системный Chrome); слот фото + 2 SVG-силуэта. Делегировано Card Engineer (ui-ux-pro-max); Tester PASS (54 теста, превью 1080×1620 реальные), Reviewer APPROVE-WITH-NITS; убрано мёртвое поле accentBrightVar. Дирижёр глазами одобрил превью (owner checkpoint). ⚠️ Данные seed: счёт перекошен (напр. Ballon d'Or Месси показывает 4 вместо 8) — на сверку владельцу (verified:false). Гейты: typecheck ✓ lint ✓ test 54/54 ✓ build ✓.
- 2026-06-21 Фаза 3 (P3-1..P3-5) — Studio: per-player селекторы 4 срезов + same-age, реактивный scale-to-fit предпросмотр (in-memory), Download PNG (blob через /api/card) + Share (Web Share API с фолбэками), RU/EN. Делегировано UI Engineer (ui-ux-pro-max); Tester PASS (54 теста + e2e 1 passed на системном Chrome, скриншоты desktop/mobile), Reviewer APPROVE-WITH-NITS (нитки → в Фазу 4). Дирижёр одобрил UI глазами (desktop 2-кол, mobile ведёт карточкой). Гейты: typecheck ✓ lint ✓ test 54/54 ✓ build ✓ e2e ✓.
- 2026-06-21 Фаза 4 (P4-1,P4-2) — UI-моушн (stagger/fade/press, всё под useReducedMotion), скелетон+кроссфейд превью, адаптив; карточка поднята до «вау» через ui-ux-pro-max (фото-glass, инсет-бары + diamond-нода, halo-футер) — статична, без моушна (PNG детерминирован). Phase-3 нитки внесены (aria-live busy, modeOf инлайн). Делегировано Design/Motion; Tester PASS (4 гейта+e2e, card/render motion-free подтверждён), Reviewer APPROVE-WITH-NITS → убран dead PressScale. Дирижёр одобрил финальный визуал. Гейты: typecheck ✓ lint ✓ test 54/54 ✓ build ✓ e2e ✓.
- 2026-06-21 Фаза 5 (P5-1..P5-3) — happy-path E2E (срез→превью→реальный PNG скачан, проверка PNG-сигнатуры+размера); финальное acceptance-ревью всего репо: 0 блокеров, подтверждены preview⇄PNG консистентность, mechanical verdict, безопасность /api/card (нет SSRF), data honesty, нет any; BUILD_REPORT.md написан. Гейты финал: typecheck ✓ lint ✓ test 54/54 ✓ build ✓ e2e 2/2 ✓. BUILD COMPLETE.
- 2026-06-22 ВИЗУАЛЬНЫЙ ОВЕРХОЛ (карточка-герой) — по фидбэку владельца («выглядит как форма, а не god-tier»). Card/Design Engineer (ui-ux-pro-max): пересборка layout — карточка крупная и доминирует на ambient-ауре (pink/blue mesh + halo + volumetric shadow), контролы → тихий glass-рейл справа (desktop) / bottom-sheet по кнопке «Customize» (mobile, full-screen карточка); premium `.glass-panel` (слоистое стекло, inner-highlight), неон-вордмарк. Карточка/рендер остались БЕЗ моушна (PNG детерминирован). Отдельный Reviewer: APPROVE-WITH-NITS (design goal met) → дирижёр убрал dead Stagger*, добавил Esc-dismiss + focus-management в bottom-sheet. Скриншоты главной 1366/390 + шторка в preview/. Гейты: typecheck ✓ lint ✓ test 55/55 ✓ build ✓ e2e 2/2 ✓. Залито на живой сайт.
- 2026-06-22 Before-launch DATA ACCURACY pass — (а) Data Engineer перекалибровал seed под документированную реальность: карьерные тоталы (голы 840/901, ассисты 380/245, матчи 1084/1271, минуты 88k/100k, трофеи 44/35), Ballon d'Or Месси 4→8 (по сезонам), Роналду 5; все verified:false; датасет 187 строк; DATA_REPORT обновлён. (б) Дирижёр нашёл и починил БАГ агрегации: трофеи и Ballon d'Or считались по уникальным ИМЕНАМ (uniq) → Месси показывал 1 ЗМ и трофеи=типы. Введены trophyCount (distinct season+name = выиграно) и ballonDor (distinct сезоны) → карточка теперь Трофеи 44:35, ЗМ 8:5, итог career 6:4 Месси. Отдельные Tester (data-audit PASS) и Tester (fix-verify PASS, card-path 8/5 + 44/35, превью перегенерены). Гейты: typecheck ✓ lint ✓ test 55/55 ✓ build ✓ e2e 2/2 ✓.
- 2026-06-22 КИНЕМАТОГРАФИЧНЫЙ MOTION-ОВЕРХОЛ (Awwwards-уровень) — по фидбэку владельца («сайт-впечатление, а не рабочая форма»). Motion/Design Engineer (ui-ux-pro-max): единая motion-система (src/lib/motion/tokens.ts — custom cubic-bezier easings, durations, springs); Lenis momentum-скролл + GSAP ScrollTrigger (src/components/motion/smooth-scroll.tsx), кинематографичный hero со стаггер-ревилом (home/hero.tsx), scroll-triggered параллакс-секция (home/verdict-band.tsx), драматичный вход карточки (spring) + бары заполняются пружиной через scaleX + count-up цифр + мягкий pulse (card/card-animations.tsx, lib/motion/use-count-up.ts), плавный морф/crossfade превью при смене ЛЮБОГО среза, магнитные кнопки (motion/magnetic.tsx) + hover-tilt + анимированные тоглы/сегменты (layoutId). КРИТИЧНО: вся анимация карточки за `animated`-проп (default false) → PNG-роут `/render/card` рендерит статику, детерминизм сохранён. 60fps (только transform/opacity/filter), prefers-reduced-motion отключает ВСЁ (Lenis не инстанцируется, ScrollTrigger не создаётся). Отдельный Reviewer: APPROVE-WITH-NITS (PNG-детерминизм + reduced-motion + e2e-селекторы подтверждены) → дирижёр убрал dead rafId + unused `.studio-aura`. e2e гоняется с reducedMotion:reduce (детерминизм); шоукейс-видео + скриншоты ключевых состояний сняты с motion ON (scripts/capture-showcase.ts → preview/: hero, studio, card-updated, mobile, mobile-sheet, showcase-desktop.webm). Гейты: typecheck ✓ lint ✓ test 55/55 ✓ build ✓ e2e 2/2 ✓. Залито в origin/main (cf8867f).
- 2026-06-22 QA + docs — дирижёр верифицировал moat по кадрам видео (ffmpeg-экстракт: hero→скролл→морф карточки→магнитная кнопка→параллакс-вердикт — без дефектов); BUILD_REPORT.md обновлён под реальность (54→55 тестов, 112→187 строк, исправлен устаревший пункт про Ballon d'Or — он уже 8:5, добавлены секции про data-accuracy/visual/motion-пассы и новую preview-медиа + capture-флоу). Doc-only, гейты остаются зелёными (cf8867f кодовая база не менялась).
- 2026-06-22 Фаза 6 / Data layer (P6-1..P6-4) — Data Engineer построил контракт-слой расширения: METRIC_CATALOG (25 метрик, группы attack/creation/efficiency/discipline/trophies; новые производные G+A, G+A/90, минут/гол, SoT%, shots/90 + хет-трики как illustrative-поле схемы), selection-aware verdict (`CardSlice.metrics` упорядочен → счёт ТОЛЬКО по выбранным), стекающиеся `competitions?` (additive, backward-compat), season-trend (реальный) + ИЛЛЮСТРАТИВНЫЕ heatmap/shotmap за `DataSource` (детерминированный seed mulberry32+FNV — без RNG/clock), slice-params сериализует metrics+competitions (дефолтный URL/PNG БАЙТ-в-БАЙТ тот же). Отдельный Reviewer: APPROVE-WITH-NITS (дефолт-детерминизм, illustrative-флаги, RU/EN-паритет, контракт подтверждены) → дирижёр убрал dead `"time"` из MetricFormat. Гейты: typecheck ✓ lint ✓ test 78/78 ✓ build ✓. e2e отложен до UI-вехи (нет изменений селекторов/рендера; дефолтный PNG байт-идентичен). Дирижёр перенёс прод на свежий билд (live 0.0.0.0:3000). Coммит per данные-слой.
- 2026-06-22 Фаза 6 / Charts (P6-5..P6-7) — Charts Engineer (ui-ux-pro-max): hand-rolled SVG-компоненты под брендом (glass+неон) + чистые тестируемые билдеры. `ComparisonRadar` (buildRadarModel: нормализация outward-is-better, lower-is-better инвертирован), `SeasonTrendChart` (buildTrendModel: хронологический union сезонов, null = честный разрыв, Роналду dashed), `PositionalHeatmap`+`Shotmap` на ИЛЛЮСТРАТИВНЫХ данных. Бейдж `IllustrativeBadge` рендерится БЕЗУСЛОВНО (не проп) в обоих позиционных виджетах + в aria-label — омитить нельзя (SPEC §6). a11y: формы (не только цвет) для исходов ударов, dash для линий. Reduced-motion: финальная геометрия статична. Отдельный Reviewer: APPROVE-WITH-NITS (honesty non-bypassable, билдеры корректны с реальными тестами, render-safe). Компоненты пока не интегрированы (это P6-9). Гейты: typecheck ✓ lint ✓ test 95/95 ✓ build ✓.
- 2026-06-22 Фаза 6 / UX-редизайн контролов (P6-8 + P6-10 + табы из P6-9) — по фидбэку босса («всё в куче, нативные select дёшево»). UI Engineer (ui-ux-pro-max): КОНТЕКСТ-ВКЛАДКИ по турнирам (`CompetitionTabs` на Radix Tabs: Все/Лига/ЛЧ/Сборная/Кубки; «Кубки»=cup-трио через competitions[]; глобально на обоих игроков; анимированный gold-pill layoutId), УБРАНЫ ВСЕ нативные `<select>` → `NeonSelect` на Radix Select (тёмный неон, glass-popover с neon-pop-in анимацией, a11y/клавиатура), `StatPicker` — отдельный блок чипов по группам + пресеты + reorder + min 3 (role=switch, hatTricks помечен illustrative), чёткая иерархия (табы сверху → блоки → статы). PNG детерминирован (дефолтный slice сериализуется байт-в-байт; tab→competitions round-trips). e2e переписан под новые контролы (таб-клик, Radix-combobox, stat-chip toggle меняет счёт; PNG-сигнатура сохранена); capture-showcase обновлён. Отдельный Reviewer: APPROVE-WITH-NITS (0 native select, mapping корректен, a11y/anchors целы) → дирижёр убрал dead dep @radix-ui/react-dropdown-menu. Гейты: typecheck ✓ lint ✓ test 95/95 ✓ build ✓ e2e 2/2 ✓. Перенесён прод на новый билд (live 0.0.0.0:3000), превью desktop+mobile перегенерены. Осталось в P6-9: интеграция виджетов (радар/тренд/heatmap/shotmap) секциями.
- 2026-06-22 Фаза 6 / P6-9 ЗАКРЫТ (интеграция виджетов) — UI Engineer (ui-ux-pro-max) собрал секцию **Insights** (`src/components/studio/insights.tsx`) под картой-героем: радар (по `slice.metrics` + model) + тренд по сезонам (NeonSelect-пикер метрики) бок-о-бок; ниже 2×2 heatmap+shotmap (Месси/Роналду) с БЕЗУСЛОВНЫМИ illustrative-бейджами. Всё следует ГЛОБАЛЬНОМУ контексту турниров (`slice.messi.competitions/competition/includePenalties` — оба игрока в локстепе). Read-only/аддитивно: card-render path, slice-params, PNG-детерминизм не тронуты. +3 i18n-ключа (insightsKicker/Title/Subtitle, RU+EN). Отдельные Tester (ALL GATES PASS) + Reviewer (APPROVE-WITH-NITS). Дирижёр на owner-checkpoint (превью) поймал РЕАЛЬНЫЙ баг: `Reveal` под reduced-motion (и в headless, где framer `useReducedMotion` даёт false → ветка whileInView, чей IntersectionObserver не срабатывает на программном скролле) залипал на opacity:0 → секция невидима. Фикс: `Reveal` переведён на `animate`-on-mount (паттерн `FadeIn`), гарантированно видим во всех средах; убран dead `REVEAL_STAGGER`-алиас (nit ревьюера). Гейты на финальном коде: typecheck ✓ lint ✓ test 95/95 ✓ build ✓ e2e 2/2 ✓. Прод поднят под **pm2** (`footy`, авто-рестарт+boot-persist через systemd, detached) на 0.0.0.0:3000 — проверен auto-respawn (kill → 200 за 4с). Превью desktop 1366 + mobile 390 + секция Insights перегенерены. ФАЗА 6 ЗАКРЫТА. commit+push.
- 2026-06-23 RELEASE-ПОЛИРОВКА (после смерти прошлого прогона) — дирижёр довёл до ума ~19 несохранённых файлов: (1) **мгновенный показ студии** — `whileInView`-reveal'ы (page.tsx) залипали «чёрной серединой» при заходе → переведены на `animate`-on-mount (анимация играет вне зависимости от вьюпорта, к скроллу уже осела); reduced-motion по-прежнему гасит всё; (2) **анимация ВСКРЫТИЯ при смене ЛЮБОГО среза** — `card-preview` contentKey пересобран из канонической сериализации `paramsFromSlice` + значения строк + счёт (строгий суперсет старого ключа) → смена comp-таба/сезона/возраста/пенальти/набора статов ремаунтит `animated`-карту (бары пружинят, цифры count-up); локаль запинена (RU/EN не дёргает лишний ремаунт); (3) **mobile QA 390px** — MESSI/vs/RONALDO стекаются в столбик на мобиле (широкие Orbitron-black имена влезают), 44px тач-таргеты (segmented/tabs/lang-toggle/reorder), общий `FOCUS_RING` вынесен в один экспорт; pb для fixed «Customize» перенесён на внешний wrapper (не двоится под Insights). Гейты ПО ОЧЕРЕДИ (защита от OOM): typecheck ✓ lint ✓ test 95/95 ✓ build ✓. Дирижёр-ревью диффа (recall-biased): 0 блокеров (1 altitude-nit про местоположение FOCUS_RING — не чиню на зелёном билде). e2e: первый прогон флачил empty-h1/timeout → диагностирован OOM СРЕДЫ (зомби-Chrome от мёртвого прогона жрали RAM, exit-144), НЕ баг кода; после reap зомби-хромов + прогон 1 воркером против prod-сервера (порт 3100, чтобы `next dev` не клоббернул живой `.next`) — **e2e 2/2 PASS**. Коммит+пуш СРАЗУ по зелёным быстрым гейтам (823914e полировка, 1163863 превью). Прод: rebuild → pm2 `footy` (node-entry `next/dist/bin/next start -p 3000 -H 0.0.0.0`, systemd-persist, pm2 save) — **http://82.21.92.176:3000/ → 200**, auto-respawn проверен (kill-9 → 200). Превью desktop 1366 + mobile 390 (+studio/sheet/insights) перегенерены с motion ON против живого сервера, заверены глазами. Все фазы PLAN (0–6) закрыты.
